name: Publish to npm

on:
  push:
    tags:
      - 'shared-v*'

concurrency:
  group: npm-publish-shared-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish @tom4u-stats/shared
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # required for npm Trusted Publishing (OIDC)
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6.0.8
        with:
          version: 11.5.1

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Run tests
        run: pnpm --filter @tom4u-stats/shared run test

      - name: Build shared package
        run: pnpm --filter @tom4u-stats/shared run build

      - name: Publish @tom4u-stats/shared
        run: pnpm --filter @tom4u-stats/shared publish --access public --provenance
